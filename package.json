{
  "name": "wordle",
  "version": "1.0.0",
  "description": "",
  "main": "answers.js",
  "scripts": {
    "test": "jest",
    "publish": "mkdir -p dist && cp -r icons scripts includes dist/ && cp LICENSE manifest.json popup.html dist/ && mkdir output && cd dist && zip -r ../output/chrome-extension.zip *"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
