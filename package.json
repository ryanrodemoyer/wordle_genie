{
  "name": "wordle",
  "version": "1.0.0",
  "description": "",
  "main": "answers.js",
  "scripts": {
    "test": "jest",
    "package": "mkdir -p dist && cp -r icons scripts includes dist/ && cp LICENSE manifest.json popup.html dist/ && mkdir -p output && cd dist && zip -ro ../output/chrome-extension.zip *"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
